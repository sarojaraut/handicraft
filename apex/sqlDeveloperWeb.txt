BEGIN
    ORDS.ENABLE_SCHEMA(
        p_enabled => TRUE,
        p_schema => 'CLRNG',
        p_url_mapping_type => 'BASE_PATH',
        p_url_mapping_pattern => 'clrng',
        p_auto_rest_auth => FALSE);
    commit;
END;
/